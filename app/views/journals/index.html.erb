
<h1>Adventurer Journal</h1>
<div class="animated.bg">
<%= image_tag('cruise.png', class:'cruise') %>


</div>
  <div class="journal-container">
    <% @journals.each do |journal| %>
    <div class="journal-card">
      <%= link_to journal.title, journal_path(journal) %>
    </div>
    <% end %>
  </div>
</div>
